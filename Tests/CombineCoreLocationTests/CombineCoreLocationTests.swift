import XCTest
@testable import CombineCoreLocation

final class CombineCoreLocationTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CombineCoreLocation().text, "Hello, World!")
    }
}
